import React from 'react';
import DialogActions, {DialogActionsProps} from '@material-ui/core/DialogActions';

export const ModalActions = (props: DialogActionsProps) =>
    (<DialogActions {...props}/>);
